#include<bits/stdc++.h>
using namespace std;

void solve(vector<vector<int>> &positions,int n,int m,int k)
{
    vector<int> ans;
    vector<int> parent(n,-1);
	vector<vector<int>> matrix(n,vector<int>(m,0));
	while()
	{
	    
	}
	
}

int main(){

int n,m,k;
cin>>n>>m>>k;

vector<vector<int>> positions;
while(k--)
{
    int u,v;
    cin>>u>>v;
    postions.push_back({u,v});
}

}